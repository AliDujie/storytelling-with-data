[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "storytelling-with-data-skill"
version = "2.0.0"
description = "基于《Storytelling with Data》全书构建的专业数据可视化与数据叙事工具包"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["swd*"]
